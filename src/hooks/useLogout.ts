const useLogout = () => {
  // const [, setLocalVal] = useLocalStorage("auth", "");
  // const route = useRouter();
  // const logOut = () => {
  //   try {
  //     setLocalVal("");
  //   } catch (error) {
  //     console.error(error);
  //   }
  //   route.push("/");
  // };
  const fn = () => true;
  return fn;
};

export { useLogout };
